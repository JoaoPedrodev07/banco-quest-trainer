# Frontend (Vite). Copie para `.env` se precisar sobrescrever.
#
# Endereço da API do backend. SEM esta variável no build de produção, o site
# publicado tenta http://localhost:8000 e cai SILENCIOSAMENTE nos mocks de
# exemplo (30 questões) — o aviso de acervo aparece, mas ninguém quer descobrir
# assim. Em desenvolvimento o padrão já serve.
#VITE_API_URL=https://api.seudominio.com.br/api
